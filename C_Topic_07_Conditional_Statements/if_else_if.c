#include <stdio.h>
int main() {
    int num = 0;
    if (num > 0) {
        printf("Positive number");
    } else if (num < 0) {
        printf("Negative number");
    } else {
        printf("Zero");
    }
    return 0;
}